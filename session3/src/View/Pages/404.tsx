import React from "react";

//Componente que devuelve un mensaje a todas las páginas que define como desconocidas
const NoMatch:React.FC = (): JSX.Element =>
    <div>
    <h1>Lo siento, Página no encontrada</h1>
    </div>

export default NoMatch